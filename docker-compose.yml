
version: '2.2'

services:

  storer:
    user: storer
    image: cyberdojo/storer
    init: true
    read_only: true
    tmpfs: /tmp
    build:
      context: server
      args:
        - PORT=${CYBER_DOJO_STORER_SERVICE_PORT}
    environment:
      - CYBER_DOJO_KATAS_ROOT
